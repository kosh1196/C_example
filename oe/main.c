#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    printf("Enter a no:");
    scanf("%d",&a);
    if(a%2 == 0)
        printf("The no is even.");
    else
        printf("the no is odd");

}
